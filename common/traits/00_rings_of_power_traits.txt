# Character is the bearer of the One Ring
rop_bearer_of_one_ring = {
	name = trait_rop_bearer_of_one_ring
	index = 900

	icon = {
		first_valid = {
			triggered_desc = {
				trigger = { NOT = { exists = this } }
				desc = rop_bearer_of_the_one_ring.dds
			}
			
			desc = rop_bearer_of_the_one_ring.dds
		}
	}
	
	desc = {
		first_valid = {
			triggered_desc = {
				trigger = {
					NOT = { exists = this }
				}
				desc = trait_rop_bearer_of_one_ring_desc
			}
			
			desc = trait_rop_bearer_of_one_ring_character_desc
		}
	}

	# Make sure it is never inherited or given to created characters
	inherit_chance = 0
	random_creation = 0
	genetic = no

	good = yes
	portrait_extremity_shift = 0.30
	fame = yes

	# Bearers of the ring have their health and fertility boosted
	health = 5.0
	fertility = 0.5
}
