# TODO
single_alias[weight_multiplier_block] = {
    base = int
    ## cardinality = 0..inf
    modifier = {
        ## cardinality 0..1
        add = int

        ## cardinality = 0..inf
        alias_name[trigger] = alias_match_left[trigger]
    }
}
